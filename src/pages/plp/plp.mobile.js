import React from 'react';
import Plp from './index';

const ProductListPageMobile = ({ initialState }) => {
  return <Plp initialState={initialState}>productListPage Mobile</Plp>;
};

export default ProductListPageMobile;
