import React from 'react';
import Plp from './index';

const ProductListPageDesktop = ({ initialState }) => {
  return <Plp initialState={initialState}>productListPage desktop</Plp>;
};

export default ProductListPageDesktop;
